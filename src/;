#version

in vec3 texPos;

uniform sampler2D inTexture;
uniform int flip;

void main () {

    texturePos = texPos;
    if (flip != 0) {
        texturePos.x = texturePos.x * -1;
    }

    fragColor = texture(inTexture, texturePos);
}
